# Workflow Memory

Keep only durable, cross-task context here. Do not duplicate facts that are obvious from the repository, PRD documents, or git history.

## Current State

## Shared Decisions

## Shared Learnings

## Open Risks
- As of task_05, full `swift test` is still red for existing UI/keymap expectation mismatches in `DefaultKeymapTests` and `InspectorPaneToggleTests`; focused auth/login tests pass.

## Handoffs
